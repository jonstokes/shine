<div id="asset-edit" class="col-lg-6">
  <%= form_for(@asset) do |f| %>
    <% if @asset.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@asset.errors.count, "error") %> prohibited this asset from being saved:</h2>

        <ul>
        <% @asset.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <% if @asset.url %>
      <div class="form-group">
        <%= image_tag @asset.preview_url %>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :file_url %>
      <%= f.uploadcare_field :file_url %>
    </div>

    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_area :description, class: "form-control", rows: 5 %>
    </div>

    <div class="form-group">
      <%= f.label :caption %>
      <%= f.text_area :caption, class: "form-control", rows: 5 %>
    </div>

    <div class="form-group">
      <%= f.label :credit %>
      <%= f.text_area :credit, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :alt_text %>
      <%= f.text_area :alt_text, class: "form-control" %>
    </div>

    <%= f.submit class: "btn btn-default" %>
  <% end %>
</div>

